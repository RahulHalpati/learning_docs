# Pinned to the versions this course was verified against (2026-07-22).
# opencv-contrib-python bundles the Haar cascades AND cv2.face (LBPH).
# NOTE: OpenCV 5.x dropped the bundled cascades — stay on the 4.x line.
opencv-contrib-python==4.11.0.86
numpy>=1.26,<3
pillow>=10
# Dev/test:
pytest>=8.0
#
# Higher-accuracy alternative (heavier: compiles dlib) — optional, see Section 03:
#   face-recognition==1.3.0
