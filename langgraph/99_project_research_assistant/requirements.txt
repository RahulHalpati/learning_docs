# Pinned to the versions this course was verified against (2026-07-21).
langgraph==1.2.9
langchain-core==1.5.0
# Chat model (default) — needs OPENAI_API_KEY
langchain-openai>=1.2.1
# Optional — only needed for LANGGRAPH_LLM=ollama (free local generation):
# langchain-ollama==1.1.0
# Dev/test:
pytest>=8.0
