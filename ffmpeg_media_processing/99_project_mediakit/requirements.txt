# The toolkit itself is stdlib-only — ffmpeg does the work.
pytest==9.1.1

# Optional: transcription. Free, MIT-licensed, runs locally on CPU.
# Install when you want real captions instead of placeholder timings:
#   pip install faster-whisper==1.2.0
