# app + dev deps
boto3>=1.34
pytest>=8.0

# CLI tooling (install separately or via `make install`):
#   awscli-local      — the `awslocal` wrapper (needs awscli)
#   awscli            — the aws CLI
# OpenTofu is a separate binary (see the OpenTofu course).
